diff --git a/src/realm.h b/src/realm.h
index 4826e83f5..63ecd6025 100644
--- a/src/realm.h
+++ b/src/realm.h
@@ -1060,7 +1060,8 @@ RLM_API realm_callback_token_t* realm_add_realm_changed_callback(realm_t*, realm
                                                                  realm_free_userdata_func_t userdata_free);
 
 /**
- * Add a callback that will be invoked every time the read transaction version is going to be advaced for the realm file passed as argument.
+ * Add a callback that will be invoked every time the read transaction version is going to be advaced for the realm
+ * file passed as argument.
  *
  * @return a refresh token to remove the callback
  */
