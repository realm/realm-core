# Various builds of core for cross-commit benchmarking
/core-builds

# default results directory from the parse-hist script
/bench-hist-results

# temp working file which tracks results generated by gen_bench_hist.sh
recent_results.txt
