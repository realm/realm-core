#cmakedefine HAVE_MALLOC_H
#cmakedefine REALM_VERSION "${REALM_VERSION}"
#cmakedefine REALM_DEBUG 1
#define REALM_INSTALL_PREFIX "${CMAKE_INSTALL_PREFIX}"
#define REALM_INSTALL_INCLUDEDIR "${CMAKE_INSTALL_INCLUDEDIR}"
#define REALM_INSTALL_BINDIR "${CMAKE_INSTALL_BINDIR}"
#define REALM_INSTALL_LIBDIR "${CMAKE_INSTALL_LIBDIR}"
#define REALM_INSTALL_LIBEXECDIR "${CMAKE_INSTALL_LIBEXECDIR}"
#define REALM_INSTALL_EXEC_PREFIX "${CMAKE_INSTALL_PREFIX}"
